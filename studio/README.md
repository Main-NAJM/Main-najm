# استوديو بوفايدة — أداة العمل الخاصّة

المكان الذي يُجهَّز فيه كل فيديو **قبل** توليده: السيناريو، نصّ التعليق الصوتي،
موجّهات المشاهد بالإنجليزية، نصّ المنشور، ثم المشاريع والسعر.

ملف واحد مستقلّ (`index.html`) — لا بناء ولا حزم ولا خادم.

## التشغيل محلياً

```bash
npm run studio
```

## ما تفعله الأداة وما لا تفعله

**تفعل:** تكتب لك ما تُطعمه لأداة التوليد — الموجّه الدقيق لكل مشهد، بالمفردات
البصرية التي تفهمها هذه الأدوات، وبمقاس الشاشة الصحيح.

**لا تفعل:** لا تولّد الصورة ولا الفيديو بنفسها. التوليد يحتاج خدمة مدفوعة ومفتاحاً.
انسخ الموجّه والصقه في الأداة التي تشتغل بها.

> إن اشتركتَ يوماً في خدمة توليد، يُضاف الاتّصال بها في وحدة واحدة داخل هذا الملف —
> لأن كل المخرجات مبنية أصلاً على الشكل الذي تطلبه تلك الخدمات.

## الدخول — ثلاث طرق ولكلٍّ سببها

| الطريقة | ماذا تفعل | الحالة |
| --- | --- | --- |
| **بريد إلكتروني** | حساب حقيقي على Firebase. مشاريعك تُحفظ في حسابك فتتبعك بين الهاتف والحاسوب، ولا يقرؤها غيرك ولو عرف الرابط | ✅ يعمل — مُختبَر على المشروع الحيّ |
| **رقم هاتف** | نفس الشيء برسالة نصّية | ⏸ يحتاج تفعيل: Firebase Console ← Authentication ← Sign-in method ← Phone ← Enable (ورسائل SMS قد تحتاج خطّة Blaze) |
| **رمز الجهاز** | بديل بلا إنترنت: يفتح بيانات هذا الجهاز وحدها بلا مزامنة | ✅ يعمل دائماً |

رمز الجهاز موجود كي لا تُغلق الأداة في وجه صاحبها يوم تنقطع الشبكة أو يسقط تحميل
مكتبة Firebase. وهو **ستار لا قفل**.

## المزامنة السحابية — خطوة ناقصة

البيانات تُحفظ في `users/{uid}/studio/data`. قاعدة الأمان لهذا المسار **مكتوبة في
`firestore.rules` في هذا المستودع ومُختبَرة على المحاكي**، لكنها **لم تُنشر بعد** إلى
المشروع الحيّ — والنشر يحتاج صلاحية ليست بيد المساعد.

حتى تُنشر: الدخول بالبريد يعمل، والبيانات تبقى في الجهاز، وتقول الأداة ذلك صراحةً في
تبويب «الإعدادات» بدل أن تصمت.

**لفتحها:**
[قواعد Firestore](https://console.firebase.google.com/project/albacha-metals-fecd8/firestore/rules)
← أضف `'studio'` إلى قائمة المجموعات المسموحة ← **Publish**.

أو من سطر الأوامر، بعد `npx firebase-tools login`:

```bash
npx firebase-tools deploy --only firestore:rules --project albacha-metals-fecd8
```

للتأكّد من القواعد قبل النشر:

```bash
npx firebase-tools emulators:exec --only firestore --project albacha-metals-fecd8 "node scripts/test-rules.mjs"
```

## الخصوصية — اقرأ هذا

الصفحة منشورة على GitHub Pages، أي أن **من يعرف رابطها يفتحها ويقرأ شيفرتها**.
الذي يحمي **بياناتك** هو الحساب: قواعد Firestore تربط كل وثيقة بصاحبها، فلا يقرأ
أحدٌ مشاريعك ولو فتح الصفحة.

وما يخفي الصفحة نفسها:

- غير مربوطة من أي صفحة (`/bofaida/` لا يشير إليها، ولا الموقع في الجذر)
- تحمل `noindex, nofollow` فلا تدخل نتائج البحث

## التخزين

كل شيء في `localStorage` تحت المفتاح `bofaida.studio.v1`، ويُرفع إلى حسابك حين تدخل
بالبريد (بعد نشر القاعدة). هذا يعني:

- بلا حساب: يبقى في هذا المتصفّح وحده
- مسح بيانات الموقع يمحو النسخة المحلّية
- **خذ نسخة احتياطية** من تبويب «الإعدادات» (ملف JSON) واحفظها في مكان آمن

## الأيقونات

```bash
pip install pillow
python3 studio/icons/generate-icons.py
```

شرارة كبيرة وشرارتان صغيرتان — تختلف عن أيقونة الموقع (مثلّث التشغيل) عمداً،
فالاثنتان تجلسان في شاشة واحدة على الهاتف.

## النشر

```bash
npm run build:pages
```

الرابط: <https://main-najm.github.io/Main-najm/studio/>
