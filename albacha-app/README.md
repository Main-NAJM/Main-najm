# تطبيق مؤسسة الباشة للمعادن — إدارة الزبائن والطلبات

تطبيق ويب مستقلّ لإدارة عمل المؤسسة اليومي: سجلّ الزبائن، عروض الأسعار والطلبات بقياساتها،
الدفعات والمستحقّات، وطباعة عرض السعر والفاتورة بتنسيق A4 عربي.

يعمل على الهاتف والحاسوب من نفس الشيفرة، ويُثبَّت كتطبيق على الشاشة الرئيسية، ويعمل دون
إنترنت.

---

## ما يفعله

| القسم | الوظائف |
| --- | --- |
| **الرئيسية** | عدد الزبائن، الطلبات قيد التنفيذ، إجمالي المستحقّ، الطلبات المتأخّرة عن موعد التسليم، وآخر الطلبات |
| **الزبائن** | إضافة وتعديل وحذف، بحث بالاسم أو الهاتف، اتصال وواتساب بضغطة، ورصيد كل زبون |
| **الطلبات** | عرض سعر أو طلب، بنود بالقياس (عرض × ارتفاع × العدد) أو بالقطعة، أجرة تركيب وخصم، حالات (عرض سعر / قيد التنفيذ / جاهز / مركّب / ملغى)، تصفية وبحث |
| **الدفعات** | تسجيل دفعات جزئية بتاريخها وملاحظتها، وحساب الباقي تلقائياً |
| **المستحقّات** | المبالغ غير المسدَّدة مجمّعة حسب الزبون، مع تذكير جاهز عبر واتساب |
| **الصور** | صور القياسات قبل التنفيذ وصور العمل بعد التركيب، مرفقة بكل طلب ومضغوطة تلقائياً |
| **المخزون** | الألمنيوم والحديد والزجاج والإكسسوارات: الكميات، حدّ التنبيه لإعادة الشراء، سعر الوحدة وقيمة المخزون، وحركات الاستلام والصرف |
| **التقرير الشهري** | المبيعات والتكاليف والربح وهامشه والمحصّل فعلياً، شهراً بشهر ومجموع السنة |
| **الطباعة** | عرض سعر أو فاتورة بترويسة المؤسسة وجدول البنود والمساحات والإجماليات |
| **الموقع التعريفي** | إضافة صور أعمالك إلى الموقع العام متى شئت وحذفها، ووصف لكل صورة، ورقم الواتساب الظاهر للزوّار — بلا لمس الشيفرة ولا إعادة نشر |
| **رسائل واتساب** | إرسال عرض السعر بتفاصيله، أو إشعار «الطلب جاهز للتركيب»، أو ملخّص الحساب — نصّ جاهز بضغطة من بطاقة الطلب |
| **الإعدادات** | بيانات المؤسسة على المستندات، العملة، وتصدير/استيراد نسخة احتياطية JSON |

### طريقة التسعير

بند القياس يُحسب: **العرض × الارتفاع × العدد × سعر المتر المربّع**، وبند القطعة:
**العدد × سعر القطعة**. الإجمالي = مجموع البنود + أجرة التركيب − الخصم.

وفي التقرير الشهري: **الربح = الإجمالي − تكلفة المواد والتنفيذ** المسجّلة في الطلب (حقل
لا يظهر للزبون). المبيعات والربح يُنسبان إلى شهر إنشاء الطلب، بينما **المحصّل** يُنسب إلى
شهر الدفعة نفسها لأنها قد تُقبض لاحقاً. عروض الأسعار والطلبات الملغاة خارج المبيعات.

---

## التشغيل

من جذر المستودع:

```bash
npm install        # مرّة واحدة (يثبّت التطبيقين معاً — مساحات عمل npm)
npm run app        # يفتح على http://localhost:5180
```

بدون إعدادات Firebase يعمل التطبيق في **وضع الجهاز**: كل البيانات في المتصفّح، فيمكن
تجربته وإدخال بيانات حقيقية فوراً. لربط الحساب والمزامنة:

```bash
npx firebase-tools@13 login
npm run firebase:setup
```

ثم فعّل طرق الدخول الثلاث من الكونسول (تفاصيلها في README الجذر).

### الاختبار على محاكيات Firebase

للتجربة بلا مساس ببيانات الإنتاج:

```bash
npx firebase-tools@13 emulators:start --only auth,firestore
VITE_USE_EMULATORS=1 npm run app -w albacha-app
```

`VITE_USE_EMULATORS=1` يوجّه المصادقة وFirestore إلى المحاكي المحلي، وتُحمَّل معه قواعد
`firestore.rules` نفسها فتُختبر الصلاحيات فعلياً.

---

## البيانات في Firestore

```
users/{uid}                              ← الحقل albachaProfile: بيانات المؤسسة
users/{uid}/albachaCustomers/{id}        ← الزبائن
users/{uid}/albachaOrders/{id}           ← الطلبات، وبداخلها البنود والدفعات
users/{uid}/albachaMaterials/{id}        ← المخزون وحركاته
users/{uid}/albachaPhotos/{id}           ← صور الأعمال (وثيقة لكل صورة)

siteConfig/main                          ← إعدادات الموقع العام: المالك، الواتساب، نصّ المعرض
siteGallery/{id}                         ← صور معرض الموقع (يقرأها الزوّار بلا حساب)
```

### الموقع التعريفي: من يملكه ومن يعدّله

`siteConfig` و`siteGallery` خارج `users/{uid}` لأن الزوّار يقرأونها بلا تسجيل دخول، والكتابة
محصورة في **حساب واحد**: أوّل من يفتح صفحة «الموقع التعريفي» في التطبيق ويضغط «اربط الموقع
بهذا الحساب» يصير مالكاً، ويُثبَّت معرّفه في `siteConfig/main.ownerUid`. القواعد تمنع تغيير
المالك، وتمنع أي حساب آخر من الكتابة، وتمنع حذف وثيقة الإعدادات. لنقل الملكية لاحقاً: عدّل
`ownerUid` يدوياً من كونسول Firestore.

ويقرأ الموقعُ هذه البيانات بمفاتيح الويب التي يحقنها `site/copy-to-dist.mjs` وقت النشر من
`.env.local`. بدونها يبقى الموقع بمحتواه المكتوب في الصفحة.

الصور تُخزَّن مضغوطة داخل الوثيقة نفسها (‎data URL‎) لا في Firebase Storage، فلا تحتاج
تفعيل خدمة إضافية: تُصغَّر الصورة إلى ١٢٨٠ بكسل وتُخفَّض جودتها حتى تنزل تحت ٧٠٠ كيلوبايت
(حدّ وثيقة Firestore مليون بايت)، وتُقرأ عند فتح معرض الطلب فقط لا مع بقية البيانات.

أسماء المجموعات مسبوقة بـ `albacha` لأن نفس مشروع Firebase يستضيف تطبيق «حرفة برو» الذي
يستعمل `users/{uid}/orders` بمخطّط مختلف — فالفصل يمنع اختلاط البيانات. قواعد الأمان في
`firestore.rules` بجذر المستودع تسمح لكل مستخدم ببياناته وحدها.

---

## النشر

يُنشر مع بقية المشروع على مسار `/app`:

```bash
npm run deploy -- --project albacha-metals-fecd8     # من جذر المستودع
```

| العنوان | المحتوى |
| --- | --- |
| `https://albacha-metals-fecd8.web.app/` | تطبيق حرفة برو |
| `https://albacha-metals-fecd8.web.app/albacha` | الموقع التعريفي |
| `https://albacha-metals-fecd8.web.app/app` | **هذا التطبيق** |

`npm run build` في الجذر يبني الثلاثة وينسخها إلى `dist`. بعد النشر افتح `/app` من هاتفك
وثبّته: قائمة ⋮ ← «تثبيت التطبيق» في أندرويد، أو المشاركة ← «إضافة إلى الشاشة الرئيسية»
في iPhone.

عند تعديل التطبيق بعد النشر ارفع رقم `VERSION` في `public/sw.js` حتى تصل النسخة الجديدة
لمن ثبّتوه.
